# 多参数监护仪工程样机 G0 准入审查与合同阻塞项消除报告

**文件编号：MMP-REP-G0-001　｜　版本：v1.1（按 v0.7 修订）　｜　编制日期：2026-09-18**  
**当前上位基线：[最终总合同 v0.7](<多参数监护仪_总开发合同与系统方案_v0.7 .md>)；原报告对应早期 v0.1。**  
**当前 P 合同：[第一阶段 v0.2](P/01_第一阶段_MQTT云端/合同/P_第一阶段_GatewayC云端Web执行方案合同_v0.2.md)。早期专项合同已被替代。**<br>

> 2026-09-20 复核修订：E02 与下一步协议按 v0.7 更正。原报告的单主控引脚表保留为历史参考，不构成三节点方案的施工授权，也不表示本轮重新验证了所有硬件。
**代码仓库：`Serendipity-min/medical-multi-parameter-monitor` (Private)**

---

## 1. G0 准入审查背景与目的

在《多参数监护仪工程样机总开发合同》（MMP-SOW-001）第 1.2 节中，列出了立项启动前必须消除的 9 项关键资料冲突与技术不确定性（E01 ～ E09），同时总合同与专项合同指出：**“尚未完成库内全部 PDF、DOC、开发板原理图及供应商协议的逐页复核，所有候选引脚与供应商命令属于待核验项，不得直接作为施工基线”**。

为此，技术团队对仓库内全部传感器原理图 PDF、芯片数据手册、开发板硬件手册、串口屏指令集及通信示例源码进行了**逐页复核、反编译提取与交叉比对**，形成了本报告。

本报告旨在逐一消除 E01 ～ E09 阻塞项，冻结无冲突引脚表、外设通信协议、数据帧语义、云端扩展方案，为正式进入 G1（需求签署）与 G2（方案冻结）提供坚实的技术依据。

---

## 2. 合同阻塞项（E01 ～ E09）逐项关闭清单

| 阻塞编号 | 合同核验发现的问题 | 逐页复核结论与技术裁定 | 消除状态 | 对应支撑文件与页码 |
| :---: | :--- | :--- | :---: | :--- |
| **E01** | 初拟拓扑把 ECG 到 F407 误写为 UART；ADS1292R 原生为 SPI。 | **裁定**：ADS1292R 核心为 4 线制硬件 SPI（SCK、DIN、DOUT、CS）+ DRDY 下降沿外部中断 + START + PWDN 控制线。彻底废除 UART 拓扑。 | **已关闭** | `doc/B/01_ECG_RESP_ADS1292R原理图与驱动技术手册.md`<br>（依据：`ADS1292模块电路原理图.pdf` 第 1 页、TI `ADS1292R.pdf` 第 11 页） |
| **E02** | README 混用 `AFE4490 / HKS-12C`；血氧教程实为 AFE4490 Arduino 盾板。 | **裁定**：澄清两款不同方案：① AFE4490 为 TI 纯模拟前端芯片（SPI 驱动，需 MCU 运行 LED 采样与 PPG 计算）；② HKS-12C 为带独立 MCU 的 UART 协议成品模组（115200 8N1，0xC7 类别码，直接输出 50Hz 脉搏波与 SpO2/PR）。最终实物方案已确认为 **AFE4490 Arduino，采用 SPI + DRDY/控制信号**；HKS-12C 仅为历史对比，不是当前样机选型。 | **已关闭** | `doc/A/02_SpO2_AFE4490与HKS12C血氧传感器对比与协议技术手册.md`<br>（依据：`afe4490-datasheet.pdf`、`医用传感器组合模块R1.17.pdf` 第 27 页） |
| **E03** | 温度资料涉及 GY-641V3、MLX90614，I2C 出现 `0xA4` 地址歧义。 | **裁定**：实物模组带板载 MCU 协处理器。I2C 通信时从机 8 位写地址为 `0xA4`（对应标准 7 位地址为 `0x52`）；读寄存器 `0x07` 获取 7 字节，换算公式为 `(MSB << 8 \| LSB) / 100.0`。非原生 MLX90614 SMBus（7位地址 `0x5A`），协议已完全澄清。 | **已关闭** | `doc/B/04_TEMP_GY641V3红外测温模块协议与驱动技术手册.md`<br>（依据：`GY614615V3协议说明.doc`、`STM32_IIC/user/main.c`） |
| **E04** | ECG 的 PWDN 与 UART5_RX 同时分配了 `PD2` 引脚，存在引脚冲突。 | **裁定**：重新规划引脚表。将 ADS1292R 的 PWDN 调整为 `PE8`（与开发板例程原生保持一致），`PD2` 专用于 UART5_RX。彻底消除硬件冲突。 | **已关闭** | 本报告第 3 节《冻结引脚映射表》 |
| **E05** | 血压资料含 HKB-08A、HKB-08B+，波特率与停机指令待定。 | **裁定**：HKB-08 系列采用一致的串口协议：波特率 115200 8N1，帧头 `0xFF 0xC0`，`0xA0` 启动加压并 10Hz 回送实时压，`0xAC` 帧返回收缩压/舒张压/心率（`SSYH.7=1` 标示心律不齐），紧急停机放气为 2 字节 `0xFF 0x00`。 | **已关闭** | `doc/A/03_NIBP_HKB08系列血压模块原理图与通信协议技术手册.md`<br>（依据：`HKB-08B+血压传感器V2.2.pdf` 第 3～5 页） |
| **E06** | 串口屏 vs 网页大屏定位、云端部署要求不明。 | **裁定**：① 局域网 Web 网页大屏（1080p）为首版必交交付物；② 大彩 5 寸串口屏作为本地实时走纸终端，共存运行；③ 补充腾讯云服务器 + 域名 + SSL 证书安全接入规范，满足远程监护扩展需求。 | **已关闭** | `doc/P/99_历史/04_云端与网络部署/01_腾讯云服务器_域名解析与SSL证书配置方案.md`<br>`doc/03_模块硬件与原理图技术规范/05_HMI_大彩串口屏指令集与波形控件技术手册.md` |
| **E07** | 宣传语出现“高精度医疗级”“无延迟”等不合规断言。 | **裁定**：全面纠偏。所有指标定位为“工程样机待验证指标”，在未通过受控台架和校准源对比前，不宣称“医疗精度达标”。 | **已关闭** | 总合同与专项合同已同步修正 |
| **E08** | 将 ADS1292R 描述为需要另配独立外置 ADC。 | **裁定**：ADS1292R 本身即为集成 24 位 Delta-Sigma ADC 的高精度模拟前端，无需采购或外挂额外 ADC。 | **已关闭** | `doc/B/01_ECG_RESP_ADS1292R原理图与驱动技术手册.md` |
| **E09** | 三电极、三导联、双 ADC 通道概念混淆。 | **裁定**：严格规范生理学与电气概念：三根物理电极（RA、LA、RL）构成标准单导联（Lead I 或 Lead II，由跳线/贴片决定）+ 右腿驱动（RLD）；ADS1292R 内部双通道：通道 1 专用于高频呼吸阻抗解调，通道 2 专用于心电波形采集。 | **已关闭** | `doc/B/01_ECG_RESP_ADS1292R原理图与驱动技术手册.md` |

---

## 3. 历史单主控引脚分配表（不作为 v0.7 施工基线）

下表为早期单主控方案留档，含已被替代的 UART 血氧条目。v0.7 的 Node-A / Node-B / Gateway-C 引脚须在各自阶段重新核对；本次不擅自冻结第二阶段 CANopen 引脚。

```csv
模块名称,信号名称,STM32引脚,I/O方向,电平规范,外设复用,中断/DMA通道,排针位置,审核结论
ADS1292R,SCLK,PB3,输出 (Push-Pull),3.3V,SPI1_SCK,SPI1_TX DMA2_Stream3_Ch3,P8 Pin 43,已复核无冲突
ADS1292R,MISO (DOUT),PB4,输入 (Pull-Up),3.3V,SPI1_MISO,SPI1_RX DMA2_Stream0_Ch3,P8 Pin 46,已复核无冲突
ADS1292R,MOSI (DIN),PB5,输出 (Push-Pull),3.3V,SPI1_MOSI,SPI1_TX DMA2_Stream3_Ch3,P8 Pin 45,已复核无冲突
ADS1292R,CS,PD0,输出 (Push-Pull),3.3V,GPIO_Output,-,P9 Pin 20,已复核无冲突
ADS1292R,START,PD1,输出 (Push-Pull),3.3V,GPIO_Output,-,P9 Pin 19,已复核无冲突
ADS1292R,PWDN/RESET,PE8,输出 (Push-Pull),3.3V,GPIO_Output,-,P9 Pin 15,已解决PD2冲突
ADS1292R,DRDY,PE9,输入 (Pull-Up),3.3V,EXTI9,EXTI9_5_IRQn (下降沿),P9 Pin 14,已复核无冲突
血氧模组,UART_TX,PC10,输出 (AF-PP),3.3V,UART4_TX,DMA1_Stream4_Ch4,P8 Pin 35,已复核无冲突
血氧模组,UART_RX,PC11,输入 (Pull-Up),3.3V,UART4_RX,DMA1_Stream2_Ch4,P8 Pin 38,已复核无冲突
血压模块,UART_TX,PC12,输出 (AF-PP),3.3V,UART5_TX,DMA1_Stream7_Ch4,P8 Pin 37,已复核无冲突
血压模块,UART_RX,PD2,输入 (Pull-Up),3.3V,UART5_RX,UART5_IRQn,P8 Pin 40,已保留专供UART5
红外体温,I2C_SCL,PB8,开漏输出 (OD),3.3V,I2C1_SCL,板载4.7k上拉,P8 Pin 49,已复核无冲突
红外体温,I2C_SDA,PB9,开漏输出 (OD),3.3V,I2C1_SDA,板载4.7k上拉,P8 Pin 52,已复核无冲突
ESP8266,AT_TX,PB10,输出 (AF-PP),3.3V,USART3_TX,DMA1_Stream3_Ch4,板载P1插座,跳线P5短接3-5
ESP8266,AT_RX,PB11,输入 (Pull-Up),3.3V,USART3_RX,DMA1_Stream1_Ch4,板载P1插座,跳线P5短接4-6
大彩串口屏,HMI_TX,PA2,输出 (AF-PP),3.3V,USART2_TX,DMA1_Stream6_Ch4,P9 Pin 36,跳线P3短接1-3
大彩串口屏,HMI_RX,PA3,输入 (Pull-Up),3.3V,USART2_RX,DMA1_Stream5_Ch4,P9 Pin 34,跳线P3短接2-4
蜂鸣器告警,BEEP,PF8,输出 (Push-Pull),3.3V,GPIO_Output,S8050 NPN高电平驱动,板载蜂鸣器,已复核
状态LED,DS0,PF9,输出 (Push-Pull),3.3V,GPIO_Output,低电平点亮,板载LED0,已复核
系统调试,USB_TX,PA9,输出 (AF-PP),3.3V,USART1_TX,板载CH340C,板载USB_232,跳线P7短接1-3
系统调试,USB_RX,PA10,输入 (Pull-Up),3.3V,USART1_RX,板载CH340C,板载USB_232,跳线P7短接2-4
```

---

## 4. 技术文档体系与解析交付清单

为保证后续开发工作井然有序，杜绝任何模块信息模糊，技术团队在 `doc/` 目录下建立了分层明确的规范文档库：

```text
doc/
├── README.md                              # 总索引
├── 总合同、总体方案、全系统报告与草稿      # 根目录
├── P/
│   ├── README.md
│   ├── 01_第一阶段_MQTT云端/              # 合同、设计与接口、硬件核对、验收
│   ├── 02_第二阶段_CANopen可靠性/          # 合同及本阶段交付
│   ├── 90_维护/                           # 日志与运行维护
│   └── 99_历史/                           # 被替代的合同与 WSS 资料
├── A/                                     # Node-A：血氧、血压
├── B/                                     # Node-B：心电、呼吸、体温
└── 03_模块硬件与原理图技术规范/            # 共用参考
```

---

## 5. 审查结论与后续推进建议

* **准入审查结论**：保留原 G0 审查记录；本轮关闭 E02 选型及协议方向的文档冲突。不得将历史表中的“已关闭”推导为 v0.7 三节点硬件已全部验收。
* **下一步推进指令**：
  1. 团队以 v0.7 最终总合同、当前专项合同及 `doc/A/`、`doc/B/`、`doc/P/` 与 `doc/03_模块硬件与原理图技术规范/` 为执行基线，不再引用旧版非受控散落资料；
  2. 后续协议基线为 **CANopen Object Dictionary / PDO + MQTT Topic/Data Model**，不再锁定 MMP/1。第二阶段的实际执行状态以当前用户授权和 P 阶段索引为准；
  3. 执行 Git 提交并推送至 GitHub 私有仓库，完成资料归档与基线固化。
