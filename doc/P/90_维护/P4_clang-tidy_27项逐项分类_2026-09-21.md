# P4 clang-tidy 27 项逐项分类

原始批次：`pr-2026-09-20T155858.095798_0000-c00a42`，原 SHA `ec75b10c885b066f6a7bd6142cc1a9cf98b6affa`。复核基线：`44e3bdf74c1d503abefc992406700ea719ab346c`。

复核方式：Codex 逐项阅读源码、参数、对象容量和相关调用者，不运行批量自动修复。分类不是项目负责人签署的风险接受；没有修改 accepted-findings 或 clang baseline。

18 项 `FP_CONTEXT`：当前调用上下文未成立的通用缓冲区告警；8 项 `MAINTENANCE_REVIEW`：保留易混参数维护事项；1 项 `PORTABILITY_REVIEW`：保留指针表示的平台假设。工具原始 MEDIUM 等级保留，不能换算成 27 个已确认安全漏洞，也不自动让门禁忽略这些提示。

| # | Finding ID / 规则 | 相对位置 | 分类 | 证据 | 后续处理 |
|---|---|---|---|---|---|
| 1 | `325e582a26a8af357f406984` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/canopen/src/mp_adapter.c:7` | FP_CONTEXT | mp_adapter_init 清零完整 MpAdapter，长度为目标对象 sizeof；main.c、emit.c、pipeline.c 传入实际对象地址。 | 保留初始化，变更对象所有权时重新核对。 |
| 2 | `25a3c06de08bf993b68c5c78` / `bugprone-easily-swappable-parameters` | `gateway/canopen/src/mp_adapter.c:12` | MAINTENANCE_REVIEW | frame 的 node 与 stream 可隐式转换，但当前调用明确传入节点 1/2/3 和 MpStream；未见实参交换。 | 未来扩展节点时考虑参数结构或更强类型；不改现有内部调用。 |
| 3 | `627e1d08af51b10ef43926b4` / `bugprone-easily-swappable-parameters` | `gateway/canopen/src/mp_adapter.c:26` | MAINTENANCE_REVIEW | capture 的 base 与 tick 都是整数；wave/poll 分别传 OD 基址和采样 tick，未发现交换。 | 保留语义命名，新增调用须复核时间单位与参数顺序。 |
| 4 | `14b5943bab9aea2a0f53d912` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/canopen/src/mp_adapter.c:84` | FP_CONTEXT | poll 的 node 循环固定 1..2；清零 seq[node-1] 的长度等于一行 sizeof，索引和长度匹配。 | 若增加节点数，同步复核数组维度及循环界限。 |
| 5 | `28c3a7a5d5025a72502fb1b3` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/canopen/src/mp_can_driver.c:12` | FP_CONTEXT | mp_bus_reset 对静态 ports[3] 按 sizeof ports 清零，无外部长度参与。 | 缓冲区边界提示不成立；指针表示可移植性另见同位置专门条目。 |
| 6 | `d6e4b255b6ab2ab2eebcfdac` / `bugprone-multi-level-implicit-pointer-conversion` | `gateway/canopen/src/mp_can_driver.c:12` | PORTABILITY_REVIEW | ports 数组转换为多级指针后传入 memset 的 void*；只按字节写对象，没有作为 void** 解引用。零字节作为空指针表示依赖当前目标 ABI。 | 不是已确认越界；跨 ABI 移植时可逐项赋 NULL，须独立提交和回归。本轮不做消警强制转换。 |
| 7 | `76164cd51793ce15741837f8` / `bugprone-easily-swappable-parameters` | `gateway/canopen/src/mp_can_driver.c:99` | MAINTENANCE_REVIEW | CO_CANmodule_init 的 nt/rate 均为 uint16_t；这是上游驱动接口。CO_CANinit 传固定计数和 500k 速率，rate 另有检查。 | 保留上游签名；集成新端口时对照 RX/TX 数量及速率。 |
| 8 | `31f8ff7a24e3cabe5543109d` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/canopen/src/mp_can_driver.c:106` | FP_CONTEXT | CO_CANmodule_init 先检查 m 非空，然后按 sizeof(*m) 初始化上游已分配模块。 | 不将普通结构初始化误当秘密清理；保持现有写入范围。 |
| 9 | `988c907ef764730d1945448a` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/canopen/src/mp_can_driver.c:107` | FP_CONTEXT | rx 初始化大小来自 nr。CANopen.c:653 按 CNT_ALL_RX_MSGS 分配相同元素数，:937 传相同计数；当前调用链容量一致。 | 该结论限定上游已核对的调用者；新增调用者必须证明 rx 容量。 |
| 10 | `dfe83d028288a5205311f60c` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/canopen/src/mp_can_driver.c:108` | FP_CONTEXT | tx 初始化大小来自 nt。CANopen.c:656 按 CNT_ALL_TX_MSGS 分配，:937 传相同计数；当前调用链容量一致。 | 新增调用者须证明 tx 容量；不因规则建议改用非可移植 *_s API。 |
| 11 | `94ba96409d54bc92f17f4f6d` / `bugprone-easily-swappable-parameters` | `gateway/canopen/src/mp_can_driver.c:140` | MAINTENANCE_REVIEW | CO_CANtxBufferInit 的索引/标识以及相邻标志参数存在类型兼容，签名由 CANopenNode 定义；本次未确认交换实参。 | 保留协议驱动 ABI，后续端口适配逐调用核对；不机械重排参数。 |
| 12 | `e818a576f6e0b35f8d48a13d` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/canopen/src/mp_can_driver.c:146` | FP_CONTEXT | tx 指向 m->txArray[i]；前置 i < txSize 检查通过后才按 sizeof(*tx) 清零。 | 保留单元素初始化和前置索引检查。 |
| 13 | `e9cb937f6135b7a38e3d2938` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/canopen/src/mp_can_driver.c:170` | FP_CONTEXT | CO_CANsend 两侧 data 均为 uint8_t[8]，固定复制 8 字节；head 以 128 取模且队满先返回。 | 不把固定容量帧复制认定为无界字符串复制；后续更改帧布局需同步检查。 |
| 14 | `70d423ba3d5de76c0c95b2c0` / `bugprone-easily-swappable-parameters` | `gateway/canopen/src/mp_od.c:7` | MAINTENANCE_REVIEW | var 的 index/size/value 均为兼容整数，调用限 mp_od_init 中冻结对象表，字段值与容量池写入一致。 | 后续 OD 扩展考虑具名表项，现阶段不为提示重构。 |
| 15 | `4870d0bc41f9a93310d7d8ea` / `bugprone-easily-swappable-parameters` | `gateway/canopen/src/mp_od.c:20` | MAINTENANCE_REVIEW | rec 的 index/count 存在隐式转换；application/mp_od_init 的实参按索引、元素数、长度表、值表顺序传入。 | 新映射须同时核对 count 和两张表长度，保留人工复核项。 |
| 16 | `ce7b357ee3b0300d57d4a9e6` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/canopen/src/mp_od.c:68` | FP_CONTEXT | mp_od_init 的 o 为 init_node 中实际 MpOD 成员，按 sizeof(*o) 初始化完整对象。 | 当前调用链无外部长度；保留初始化。 |
| 17 | `c908a802ce5e43e18c34e7c3` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/canopen/src/mp_od.c:69` | FP_CONTEXT | cfg 指向同一 MpNode 中的 CO_config_t，按 sizeof(*cfg) 初始化；当前调用者提供实际对象。 | 保留配置初始化，新增调用者不得传入不完整存储。 |
| 18 | `32c69ec4fc4d6bbbde86ec68` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/canopen/src/mp_od.c:152` | FP_CONTEXT | mp_od_read 仅在 OD_getSub 成功且 dataLength <= 4 时复制到 uint32_t；OD backing cells 为 uint32_t 数组。 | 保留成功与长度检查，结论不推广到任意外部 OD 描述。 |
| 19 | `9062341edd933662a16c30dd` / `bugprone-easily-swappable-parameters` | `gateway/canopen/src/mp_od.c:156` | MAINTENANCE_REVIEW | mp_od_write 的 index/sub/value 相邻整数兼容；produce 中调用使用固定 OD 索引、子索引及对应值。 | 后续 A/B 采集接入需按冻结 OD 表逐调用核对；本轮不改签名。 |
| 20 | `fa6139f28506b0a2f607ba34` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/canopen/src/mp_od.c:161` | FP_CONTEXT | mp_od_write 只在 OD_getSub 成功且 dataLength <= 4 时复制 uint32_t 到内部 cells；读写长度同受约束。 | 保留边界条件及当前内部 OD 所有权约定。 |
| 21 | `ca7d9c60b726026b0ffebff9` / `bugprone-easily-swappable-parameters` | `gateway/canopen/src/mp_stack.c:7` | MAINTENANCE_REVIEW | emergency 的 id/code、reg/bit 类型相近，但接口顺序由 CO_EM_initCallbackRx 定义；当前只按 id 定位节点并保存 code。 | 保留回调 ABI，未使用参数显式忽略；勿为消警调换参数。 |
| 22 | `1dfe93cb52fce89306017d93` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/canopen/src/mp_stack.c:45` | FP_CONTEXT | mp_stack_init 按 sizeof(*s) 初始化实际 MpStack；main/host tests 均提供完整对象。 | 仅为启动初始化，不宣称支持对运行中对象任意重复初始化。 |
| 23 | `336d8cf9adb9a3b01661bc0b` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/data_model/model.c:25` | FP_CONTEXT | append 的格式串均来自本文件固定常量；vsnprintf 使用剩余长度，负值或 n >= left 时立即失败，不按截断返回值移动指针。 | 保留失败传播与剩余容量检查；并非可控格式串的已确认漏洞。 |
| 24 | `35f9d1c04d4ee43e9a7d3e02` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/data_model/model.c:112` | FP_CONTEXT | 网关状态 Topic 使用固定格式和传入容量；函数尾检查 n > 0 且 n < size 才返回有效长度。 | 保留截断判定；当前调用提供真实目标容量。 |
| 25 | `32deadee239e51f7863dfa59` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/data_model/model.c:114` | FP_CONTEXT | 节点状态/事件 Topic 格式固定，字符与后缀来自有限分支，snprintf 长度受 size 限制且统一检查截断。 | 不更改 Topic 协议；本条只评价通用缓冲 API 提示。 |
| 26 | `cfa4d80aa55191a5a9673edd` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/data_model/model.c:117` | FP_CONTEXT | 遥测/补传 Topic 使用固定格式、构造的内部帧与受限源名；snprintf 使用 size 且函数尾检查截断。 | 结论限定当前内部有效 MpFrame 调用约定，不宣称是任意不可信结构的验证 API。 |
| 27 | `86a6eaa24b0317b720d44279` / `clang-analyzer-security.insecureAPI.DeprecatedOrUnsafeBufferHandling` | `gateway/storage/router.c:7` | FP_CONTEXT | mp_router_init 对实际 MpRouter 按 sizeof(*r) 初始化，队列与计数器同属该对象。 | 保留整体初始化；并发与资源生命周期仍沿用单线程约定。 |

当前源文件的行号与原始批次一致。已检查初始化调用、CANopenNode 计数分配与调用配对，以及固定数据结构的边界。未覆盖的 ARM 编译单元、真三板和长期稳定性不因本次分类获得通过。

以后新增调用者、节点、OD 映射或更换 ABI 时须重看对应条目；需要接受残余风险时由 P 单独签署 owner、理由、有效期和复核入口。本轮没有代签。
