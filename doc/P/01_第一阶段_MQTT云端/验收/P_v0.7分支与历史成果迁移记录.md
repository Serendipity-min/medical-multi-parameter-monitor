# v0.7 分支与历史成果迁移记录

日期：2026-09-20。

## 授权与范围

用户明确允许将 v0.7 最终总合同归档到 main，再创建 `dev/p-gateway-cloud-v07`，
完整迁入 `codex/p-cloud-phase1` 的内容后废弃旧分支。
本次 main 仅增加最终总合同，不合并旧云端代码、文档整理或其他本地修改。

## 历史关系

- 原 main：`533eeb2be4b7a59fed69f898cbc149bf2bd98a20`。
- 合同归档后的 main：`50699048010c57ba10f8573461844c525b022a07`。
- 旧开发分支最终提交：`875b144c63d52a4f6858a813556b4c9d16b40da7`。
- 新分支合并点：`7f50888864b4fa5525fa7de57aa0827507b36a8e`。

新分支从合同归档后的 main 建立，再通过普通 merge 保留旧分支两个开发提交及完整祖先关系。
未 squash、未重写历史、未强推；迁移合并点相对旧分支只有新增 v0.7 合同。
旧分支删除不丢失其代码、四模块 UI、部署模板、历史验收证据或提交记录。

P/A/B 文档整理与 P 合同 v0.1/v0.2 在新开发分支归档；用户已有 A/B 技术内容编辑、
旧总方案移入草稿的本地修改和未跟踪草稿继续保留，不在本次迁移中擅自提交或撤销。

## 验证

- Git 祖先检查：旧分支最终提交完整包含在新分支历史中。
- main 变更范围检查：仅新增 `doc/多参数监护仪_总开发合同与系统方案_v0.7 .md`。
- `pytest cloud/backend/tests -q`：14 项通过；依赖库有两条弃用提示，无测试失败。
- `npm --prefix cloud/web run build`：TypeScript 检查与 Vite 构建通过。
- 本轮未变更服务器部署、Nginx、证书或防火墙。

## 当前能力与后续范围

上述验证仅说明旧 WSS 云端实现迁移完整，不代表 MQTT/TLS 新合同已完成。
后续工作以 [P 合同 v0.2](../合同/P_第一阶段_GatewayC云端Web执行方案合同_v0.2.md) 为准：
Mosquitto、MQTT Adapter、Mock MQTT Publisher、按流状态模型、浏览器数据语义及
Gateway-C/ESP8266 网络出口。RR 正式算法与真实 CANopen 接入仍不属于本阶段主线交付。

服务器和主域名子路径沿用既有项目；TCP 8883 放行仍由用户操作。
Gateway-C 真机验收需要确认板型、AT 固件、串口和烧录器，不能将 PC 模拟结果写成硬件通过。
