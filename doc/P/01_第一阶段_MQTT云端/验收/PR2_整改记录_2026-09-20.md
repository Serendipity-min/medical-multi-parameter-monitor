# PR #2 首轮复核整改记录

日期：2026-09-20。对应收到的 [复核报告原文](PR2_首轮合同复核报告_2026-09-20.md)。
本轮只整改第一阶段、整理合同并建立维护日志；第二阶段未启动，未触碰开发板。

| 复核项 | 本轮处理 | 验证/边界 |
|---|---|---|
| P0-1 G0 选型和协议冲突 | E02 明确 AFE4490 Arduino / SPI + DRDY/控制；后续协议改为 CANopen OD/PDO + MQTT 数据模型 | 历史单主控引脚表注明不作 v0.7 施工基线，不擅自冻结第二阶段引脚 |
| P0-2 最小 CI | GitHub Actions 固定 Action commit，Python 3.12 测试与 Node 22 构建 | 不连接真实服务器/开发板、不放凭据、不自动部署或合并；实际 CI 结果以 PR checks 为准 |
| P1-1 assert 门禁 | root、release 名、SHA-256、已有目录、ZIP 路径/符号链接全部改显式检查 | 6 种失败场景实际在 python -O 子进程拒绝；合法包预检不写目标 |
| P1-2 运行日志 | Broker error/warning/notice 白名单转换；Backend 状态/计数事件；文件和 journal 双存 | 每来源 5MiB 当前 + 7 份轮转；运行原文和载荷不落盘，日志样本已保存 |
| P1-3 clean_session=True | 明确停机期间不保存该会话 QoS1 消息、retained 不等于历史 | 本轮保留第一阶段行为，持久 Session/缓存方案不提前实施 |
| P1-4 共享 View Token | 明确仅用于合成数据，真实人体数据前须完成受控网络或独立可撤销认证 | 本轮不接入真实人体数据，不悄悄改变认证/撤销现有凭据 |

## 已完成验证

- Backend + 运维回归：25 项通过（原 16 项 + 9 项部署/日志回归），2 项已有依赖弃用警告。
- 服务器 Broker：11 项全部通过，含 TLS、匿名/错误口令拒绝、ACL、QoS1、Retain、LWT。
- agent-browser：19 项全部通过，含 Backend/Broker 重启、页面重连、来源隔离、失效状态和移动布局。
- 日志在项目服务重启后仍保留；文件权限 Broker 640、Backend 600。HUP 转发后 Broker 保持 active，TLS 功能通过。
- 报告原文与用户提供文件字节哈希一致。归档仅移动/更新导航；A/B 与用户草稿改动不纳入提交。

证据目录：[cloud/evidence/pr2-review1](../../../../cloud/evidence/pr2-review1)。
日志策略和排障见 [维护文档](../../90_维护/运行日志与排障.md)。

## 发布与回滚

新发布 mqtt-v07-review1-20260920-01，前版 mqtt-v07-20260920-01 保留。更新独立项目 Broker/Backend unit 和 Broker 配置，短时重启后 health 已恢复。
新日志位于 /opt/medical-monitor/logs，不随 release 切换清除；备份位于同名 backups 目录。
回滚必须同时恢复 current、两个 unit 和 mosquitto.conf，再 daemon-reload/restart；详见 [部署说明](../../../../cloud/deploy/README.md)。

## 文档分类与未完成项

P 文档分 01_第一阶段_MQTT云端、02_第二阶段_CANopen可靠性、90_维护、99_历史。
旧合同从活动目录移出保留追溯；第二阶段合同单独归档并标注未启动。
24h 稳定性、真实传感器、正式 RR、CANopen 与离线缓存未在本轮实施。
CI 成功不等于最终人工 Review，也不自动授权合并 main 或执行第二阶段。
