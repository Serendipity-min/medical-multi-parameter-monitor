# 多参数心电监护仪系统样机开发项目 (Multi-Parameter Patient Monitor System)

> 当前基线为 [v0.7 最终总合同](<doc/多参数监护仪_总开发合同与系统方案_v0.7 .md>)，
> P 执行 [第一阶段 v0.2 合同](doc/P/P_第一阶段_GatewayC云端Web执行方案合同_v0.2.md)，
> 工作分支为 `dev/p-gateway-cloud-v07`。下方旧硬件与 WSS 描述保留作历史参考，
> 与新合同不一致时以新合同为准；MQTT 迁移尚未完成。

> **项目属性**：医疗健康监测系统样机研发 / 软硬件一体化系统工程  
> **版本**：v1.1.0 (Prototype - G0准入完成)  
> **主控平台**：STM32F407ZGT6 (ARM Cortex-M4F @ 168MHz)  
> **核心体征**：心电 (ECG) / 呼吸率 (RESP) / 血氧饱和度 (SpO2) / 无创血压 (NIBP) / 体温 (TEMP)  
> **远程接入**：局域网 Web 大屏 (1080p 60 FPS) + 腾讯云服务器 (HTTPS / WSS 安全网关)

---

## 1. 项目简介与背景

本项目为一款面向家用健康监护、基层医疗初筛及病房床旁辅助监护的**高精度多参数生理体征监护仪样机系统**。设备通过医用级生物模拟前端与高灵敏度传感模组，同步无创采集人体五大生命体征数据，经主控微控制器执行高精度数字信号滤波与特征解析算法后，在 5 寸工业级智能串口屏与 1080p 网页大屏上进行动态波形平滑走纸与看板显示，并支持声光越限告警与腾讯云/局域网加密远程传输。

### 核心监测参数与技术指标
| 生理参数 | 测量方法 / 芯片方案 | 测量范围 | 分辨率 / 精度 | 采样与刷新率 |
| :--- | :--- | :--- | :--- | :--- |
| **心电 (ECG)** | ADS1292R 双通道 24位 AFE，三导联电极 | 30 ～ 250 bpm | 1 bpm (工程样机精度 ±1 bpm) | 250 SPS |
| **呼吸率 (RESP)** | 阻抗呼吸测量 (高频载波调制) + ECG 基线衍生 | 8 ～ 60 次/分 | 1 次/分 (工程样机精度 ±3 次/分) | 250 SPS 内部解调 |
| **血氧 (SpO2)** | HKS-12C / AFE4490 双波长透射式 PPG | 70% ～ 100% | 1% (工程样机精度 ±2%) | 50 Hz 脉搏波 |
| **脉率 (PR)** | 光电容积脉搏波周期检波 | 47 ～ 255 bpm | 1 bpm | 50 Hz |
| **血压 (NIBP)** | HKB-08B+ 示波法阶梯放气测量 | 0 ～ 300 mmHg | 1 mmHg (动态精度 ±3 mmHg) | 10 Hz 压力状态机 |
| **体温 (TEMP)** | GY-641V3 (MLX90614ESF) 额温热辐射 | 25 ～ 50 ℃ | 0.1 ℃ (工程样机精度 ±0.2 ℃) | 1 Hz 周期采样 |

---

## 2. 核心技术文档与合同管理索引 (Doc Index)

文档按 P / A / B 分工归档，总合同、总体方案、全系统报告与草稿保留在 `doc/` 根目录。归档规则见[项目文档索引](doc/README.md)：

```text
doc/
├── README.md                              # 文档归档规则与总索引
├── 总合同、总体方案、全系统报告与草稿      # 保留在根目录
├── P/                                     # P：专项合同、协议、云端与集成
│   ├── README.md
│   ├── 02_项目负责人及上位机_专项工作合同_v0.1.md
│   ├── P_第一阶段_云端先行执行方案合同_v0.1.md
│   ├── P_第一阶段云端执行报告.md
│   ├── P_云端接口语义说明.md
│   ├── P_云端部署与回滚说明.md
│   └── 04_云端与网络部署/                  # 早期部署参考
├── A/                                     # A：血氧、血压与 Node-A
│   ├── README.md
│   ├── 02_SpO2_AFE4490与HKS12C血氧传感器对比与协议技术手册.md
│   └── 03_NIBP_HKB08系列血压模块原理图与通信协议技术手册.md
├── B/                                     # B：心电、体温与 Node-B
│   ├── README.md
│   ├── 01_ECG_RESP_ADS1292R原理图与驱动技术手册.md
│   └── 04_TEMP_GY641V3红外测温模块协议与驱动技术手册.md
└── 03_模块硬件与原理图技术规范/            # A/B 共用参考
    └── 05_HMI_大彩串口屏指令集与波形控件技术手册.md
```

---

## 3. 硬件系统引脚分配与无冲突映射表 (Pinmap)

经过 G0 审查严格校验，彻底解决了此前 `PD2` 在心电掉电（PWDN）与串口 5 血压接收（UART5_RX）上的引脚占用冲突（PWDN 重定向至 `PE8`）：

| 模块类别 | 芯片/模组 | 通信接口 | STM32 硬件引脚 | 复用与外设属性 | 作用说明 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **心电/呼吸** | ADS1292R | 硬件 SPI1 + EXTI | `PB3` (SCK), `PB4` (MISO), `PB5` (MOSI)<br>`PD0` (CS), `PD1` (START), `PE8` (PWDN)<br>`PE9` (DRDY 中断) | SPI1 (最高 2MHz)<br>EXTI9 下降沿中断 | 24 位模数转换，250 SPS 连续读取 9 字节数据帧；PWDN 由 PE8 控制，杜绝引脚争用 |
| **血氧/脉率** | HKS-12C (首选)<br>AFE4490 (备选) | UART / SPI | `PC10` (UART4_TX), `PC11` (UART4_RX)<br>`PB13/14/15/PD11` (备用 SPI2) | UART4 (115200 8N1)<br>DMA 接收 + IDLE 中断 | 周期接收 50Hz 光电容积脉搏波幅值、SpO2 百分比与脉率 |
| **无创血压** | HKB-08B+ | 独立串口 | `PC12` (UART5_TX), `PD2` (UART5_RX) | UART5 (115200 8N1) | 充气加压控制、10Hz 实时袖带压、0xAC 测量结果与 0xFF 0x00 紧急放气 |
| **红外体温** | GY-641V3 | 硬件 I2C | `PB8` (I2C1_SCL), `PB9` (I2C1_SDA) | I2C1 (标准模式 100 kHz) | 从机地址 `0xA4`（7位 `0x52`），读取 0x07 寄存器获取 7 字节体温数据 |
| **智能串口屏** | 大彩 5寸串口屏 | 串口 TTL | `PA2` (USART2_TX), `PA3` (USART2_RX) | USART2 (115200 8N1) | 工业级人机界面，执行 `0xB1 0x35` 多通道平滑走纸与参数看板刷新 |
| **局域网 WiFi** | ESP8266 | 串口透传 | `PB10` (USART3_TX), `PB11` (USART3_RX) | USART3 (115200 8N1) | 直插主板 P1 接口，建立 TCP Socket 或 HTTP/WSS 网关透传 |
| **声光报警** | 蜂鸣器 + LED | GPIO 推挽输出 | `PF8` (BEEP_CTRL), `PF9` (LED0) | TIM14 PWM / GPIO | 生理参数越限报警、心律失常声光警示 |
| **系统调试** | CH340C USB 转串 | 串口 | `PA9` (USART1_TX), `PA10` (USART1_RX) | USART1 (115200 8N1) | 系统运行日志打印与 FlyMcu 一键自动 ISP 固件下载 |

---

## 4. 核心通信协议与数据格式速查

### 4.1 ADS1292R 数据帧格式 (SPI 250 SPS)
每当 `DRDY` 下降沿触发中断，MCU 读取 9 个字节（72 位）：
* `Byte 1 ~ 3`：状态寄存器字（24位，含导联脱落检测位）；
* `Byte 4 ~ 6`：通道 1 呼吸阻抗调制数据（24 位二进制补码）；
* `Byte 7 ~ 9`：通道 2 心电图原始数据（24 位二进制补码，经 5～40Hz FIR 滤波输出）。

### 4.2 HKS-12C 血氧数据帧格式 (UART 50Hz)
* **启动测量**：`0xFF 0xC7 0x03 CKSUM 0xA0`
* **周期数据帧**：`0xFF 0xC7 0x06 CKSUM 0xA0 MB XY XL`
  * `MB`：光电容积脉搏波相对幅值（直接送串口屏绘图）；
  * `XY`：血氧百分比（70%～100%，`0xFF` 表示计算中/探头脱落）；
  * `XL`：脉率数值（47～255 bpm，`0` 表示未出结果）。

### 4.3 HKB-08B+ 血压数据帧格式 (UART 115200 8N1)
* **启动加压**：`0xFF 0xC0 0x03 0xA3 0xA0`
* **实时充气压力 (10Hz)**：`0xFF 0xC0 0x05 CKSUM 0xA0 QYH QYL`
  * `QYH.4 = 1` 代表捕捉到心跳脉搏；压力值 = `((QYH & 0x0F) * 256) + QYL` (mmHg)。
* **最终测量结果帧**：`0xFF 0xC0 0x08 CKSUM 0xAC SSYH SSYL SZYH SZYL XL`
  * `SYS`（收缩压）：`((SSYH & 0x7F) * 256) + SSYL`（**若 `SSYH.7 = 1` 标识检测出心律不齐！**）；
  * `DIA`（舒张压）：`SZYH * 256 + SZYL`；
  * `XL`：心率数值 (bpm)。
* **紧急停机放气**：`0xFF 0x00`（无帧头 2 字节，立即停泵并全速泄气）。

### 4.4 GY-641V3 / MLX90614 红外测温 (I2C)
* 向 `0xA4` 写入寄存器地址 `0x07`，随后连续读取 7 字节：
  * 物表温：`To = ((Byte 1 * 256) + Byte 2) / 100.0` (℃)；
  * 环境温：`Ta = ((Byte 3 * 256) + Byte 4) / 100.0` (℃)；
  * 人体体温：`Bo = ((Byte 5 * 256) + Byte 6) / 100.0` (℃)。

### 4.5 大彩串口屏曲线走纸指令
* `EE B1 35 [Screen_ID(2B)] [Ctrl_ID(2B)] [Channel(1B)] [Data_Len(2B)] [Data...] FF FC FF FF`
  * 新数据点在曲线末尾压入，旧波形自动向左平移，实现医疗级平滑走纸显示效果。

---

## 5. 医疗设备开发安全与工程规范

> [!CAUTION]
> **电气安全与抗干扰防线（生命线规范）**：
> 1. **严禁市电开关电源直连人体测试**：人体测试心电时，**必须使用电池组（2S/3S 锂电池配合低噪声线性稳压降压）或移动电源供电**。市电开关电源的 50Hz 工频漏电与高频杂波不仅会彻底湮灭微弱心电信号（0.1mV～4mV），且存在电击危险。
> 2. **大电流感性负载物理隔离**：血压模块的气泵和电磁阀打气瞬间电流高达 500mA，必须配置续流二极管（SS34/1N4007），并使用独立电源支路，防止单片机复位或 ADC 模拟参考电压波动。
> 3. **患者安全防护（BF/CF 级规范）**：电极前端串联 39kΩ 金属膜电阻与 TVS 瞬态抑制二极管，确保任何单故障状态下流入人体的漏电流不超过 10 μA。

---

## 6. 合同规划与项目推进里程碑 (Milestones)

* **阶段一：硬件架构与各传感器单体通信闭环（里程碑 1）**
  * 依据 `doc/00_G0准入审查与合同阻塞项消除报告.md` 冻结的引脚分配进行硬件接线；
  * 完成 STM32F407ZGT6 主板对 ADS1292R、HKS-12C 血氧、HKB-08B+ 血压及红外测温模组的电气连接与底层驱动；
  * 实现各模块数据校验（CRC/Checksum）与串口回传验证，输出《单模块硬件测试验收报告》。
* **阶段二：嵌入式算法集成与多任务并发调度（里程碑 2）**
  * 基于 FreeRTOS 实现多速率任务并发调度；
  * 移植 CMSIS-DSP 库实现 5～40Hz FIR 带通滤波，完成心电 R 峰检波、心率提取与呼吸率衍生算法，输出《算法性能与体征精度测试报告》。
* **阶段三：大彩智能串口屏人机界面联动与本地整机闭环（里程碑 3）**
  * 完成 5 寸专业监护仪 UI 界面绘制（心电/脉搏波实时走纸、参数看板、参数设置、声光越限告警）；
  * 实现脱机独立稳定运行，输出《整机样机功能演示与操作规程》。
* **阶段四：远程物联网传输与工程可靠性测试（里程碑 4 / 终期交付）**
  * 部署 `doc/P/04_云端与网络部署/` 所述的腾讯云 Nginx + WSS + 1080p 大屏网关；
  * 打通 ESP8266 局域网 Web / WebSocket 监护大屏数据同步；
  * 完成整机连续运行 48 小时压力老化测试与电磁兼容性（EMC）验证，整理全套技术文档并办理交付结项。
