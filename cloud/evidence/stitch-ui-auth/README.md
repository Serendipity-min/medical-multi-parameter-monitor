# 连接失败反馈与冻结接收说明

2026-09-22：按用户“认证后设置再次弹出、需要明确错误提示”的反馈完成。

- 现有 Backend 使用静态只读令牌，没有自动到期逻辑。只读核对确认本机预览与服务器使用不同令牌。没有把聊天中的令牌再次写入工具参数或记录，也不据此声称已逐字匹配用户输入。
- 服务器当前有效令牌的交付位置更正为 `E:/medical-monitor-private/server-access.json`。原 AppData 路径出现工具侧可读、用户资源管理器不可见的差异，原因尚未确认；E 盘副本已核对一致，并确认记事本窗口显示该文件标题。目录仅允许当前用户和 SYSTEM 访问，未改变服务端令牌；仓库、截图和测试数据不包含该值。
- 添加独立失败弹窗及重新输入入口；错误令牌不自动反复重试，设置和错误弹窗不会同时占据模态层。
- 数据格式不兼容单独分类，并使用浏览器允许的应用关闭码。该分类回归只在本地页面使用固定普通 JSON，不向服务器发送协议测试数据。
- 冻结仍接收快照，计数处新增内存缓冲解释，冻结状态写明“仍在接收”。不改变原缓冲策略。

验证：TypeScript / Vite 构建和 11 项前端单元测试通过。[本地专项回归](local/interaction-acceptance.json) 13 项通过；[服务器专项回归](server/interaction-acceptance.json) 12 项通过。线上只以固定无效合成字符串验证正常登录失败反馈，并用服务已有只读令牌验证成功重试，没有枚举凭据。

[普通模式失败提示](local/invalid-token-normal.png)、[全屏失败提示](local/invalid-token-fullscreen.png)、[冻结继续接收](local/connected-frozen.png)。

[服务器静态更新回执](server-activation.json)：发布 `stitch-ui-interaction-20260922-113809`，保留前一版及回滚记录，四个服务进程均不变。本次包经 `E:/Server_file` 中转，远端哈希核验后删除对应本机暂存文件。

本轮仍未执行安全门、Git 提交、GitHub 推送或合并。上一轮 113/112 项全页面交互回归属于前一静态产物，本轮不将它们重复记为新产物测试结果。
